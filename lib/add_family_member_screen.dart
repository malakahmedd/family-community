import 'package:flutter/material.dart';

class AddFamilyMemberScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Add Family Member'),
      ),
      body: Center(
        child: Text('This is the Add Family Member screen'),
      ),
    );
  }
}